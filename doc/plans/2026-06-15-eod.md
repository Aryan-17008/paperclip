# EOD — June 15, 2026

## What We Did Today

### 1. Model Router Settings UI Page
- Built `ui/src/pages/ModelRouterSettings.tsx` — config page with model dropdowns for each complexity level (Simple/Medium/Complex)
- 12 available models in dropdown (kimi-k1, kimi-k1.5, kimi-k2.5, gpt-4o, claude-sonnet-4, gemini-2.5-pro, etc.)
- Save/Reset buttons, "How it works" explainer
- Reads/writes `~/.paperclip/model-router-config.json`
- Backend: `server/src/routes/model-router-settings.ts` — GET and PATCH endpoints
- UI wired: App.tsx routes, Sidebar nav, queryKeys, API client, RoutingDashboard link

### 2. Provider/Model Selector Dropdown (Chat Input)
- Added provider/model selector to Chat input area before sending task
- Dropdown shows: Auto Route, Kimi, OpenAI, Anthropic, Gemini, Groq
- Selecting provider reveals its models (e.g., Kimi K1/K1.5/K2.5)
- Sends `provider` + `model` to `chatApi.route()`
- Files: `ui/src/pages/Chat.tsx`, `ui/src/api/chat.ts`

### 3. Model Override Dropdown (Bot Messages)
- In bot message routing details: provider badge is now clickable
- Click opens dropdown with all providers + models
- Selecting a model calls `onUpdateModel` to change the issue's model
- "Reset to Auto" option available
- File: `ui/src/components/ChatMessage.tsx`

### 4. Investigated Latency Optimization & Prompt Cache
- Found these are partially implemented in `router.ts` but not fully wired
- `recordModelLatency` is called in heartbeat, `selectModelForComplexity` uses latency data
- Prompt cache has helpers (`hashPrompt`, `promptSimilarity`) but no actual cache storage
- Identified missing: real-time performance feedback, Pareto frontier, session-level routing, rate limit handling

### 5. Typecheck Verification
- Both server and UI pass `tsc --noEmit` clean

---

## Files Modified
- `ui/src/pages/ModelRouterSettings.tsx` — new
- `ui/src/pages/Chat.tsx` — provider selector + send logic
- `ui/src/components/ChatMessage.tsx` — model override picker
- `ui/src/api/chat.ts` — added `provider`/`model` to `ChatRouteRequest`
- `server/src/routes/model-router-settings.ts` — new
- `ui/src/App.tsx` — routes added
- `ui/src/components/InstanceSidebar.tsx` — nav link added
- `ui/src/components/Sidebar.tsx` — nav link added
- `ui/src/lib/queryKeys.ts` — `modelRouterSettings` key added
- `ui/src/api/instanceSettings.ts` — `getModelRouter`, `updateModelRouter` methods

---

## Open Items
- Backend route `/api/chat/route` needs to accept `provider`/`model` params and pass to model router (not wired yet)
- Latency optimization not actively used in heartbeat flow
- Prompt cache helpers exist but no actual cache mechanism
- Real-time performance feedback, Pareto frontier, session-level routing, rate limit handling — not implemented

---

## Restart Needed
Yes — to pick up UI changes. Server restart only if wiring backend provider/model params.
